# Aventuras-CN i18n 完整性测试报告

**日期**：2026-06-01
**场景**：QA 测试 — i18n 完整性扫描
**参与成员**：QA 负责人（gstack-qa-lead）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟡 有条件通过 — 键覆盖率 100%，但存在 14 个源码引用但语言文件缺失的键，以及约 120+ 处硬编码英文
- 阻塞项：1 个 P0（`actionChoices.quickSelectHint` 缺失）
- P1 重要问题：20 个（13 个缺失键 + 1 个占位符问题 + 6 处硬编码 alert）
- 下一步：补全 14 个缺失翻译键，再逐步清理硬编码英文

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟡 条件 Go — 键覆盖完整但引用一致性有缺口 |
| 严重度分布 | 🔴 1 / 🟠 20 / 🟡 110+ / 🟢 3 |
| 关键行动项 | 15 条 |
| 建议负责人 | i18n 开发者 |

---

## 1. 各成员核心结论

### ✅ QA 负责人（i18n 完整性测试）

- **核心判断**：语言文件键覆盖率达到 100%（1334 键对齐），之前报告的 11 个缺失键已全部修复。但源码与语言文件的引用一致性存在问题 — 14 个键在源码中通过 `$_('key')` 引用但语言文件中不存在，运行时会显示原始键名。同时约 120+ 处硬编码英文未走 i18n。
- **关键建议**：优先补全 14 个缺失键（特别是 P0 的 `actionChoices.quickSelectHint`），然后逐步将 alert/title/placeholder/aria-label 中的硬编码英文替换为 i18n 调用。

---

## 2. 综合审查发现（按严重度排序）

### 🔴 P0 阻塞（1 个）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源 |
|---|--------|------|------|---------|------|------|
| 1 | 🔴 | 键缺失 | 源码引用 | `actionChoices.quickSelectHint` 源码引用但 en.json/zh-CN.json 均不存在 | 立即添加英文和中文翻译 | QA |

### 🟠 P1 重要（20 个）

#### 源码引用但语言文件缺失的键（13 个）

| # | 键 | 说明 |
|---|---|------|
| 1 | `character.editing` | 角色编辑相关 |
| 2 | `gallery.toast.saveFailed` | 画廊保存失败提示 |
| 3 | `gallery.toast.savedImages` | 画廊保存成功提示 |
| 4 | `settings.applyMainToAllConfirm` | 设置确认对话框 |
| 5 | `settings.applyMainToAllTitle` | 设置确认标题 |
| 6 | `settings.deleteProfileWarning` | 删除配置警告 |
| 7 | `story.entries.earlierHidden` | 故事条目隐藏 |
| 8 | `story.entries.laterHidden` | 故事条目隐藏 |
| 9 | `story.entries.showMore` | 显示更多 |
| 10 | `storyCard.updated` | 故事卡片更新 |
| 11 | `vault.promptPacks.templateError` | 模板错误 |
| 12 | `vault.promptPacks.variableWarning` | 变量警告 |
| 13 | `wizard.configureProvider` | 向导配置 |

#### 占位符问题（1 个）

| # | 键 | 问题 |
|---|---|------|
| 1 | `settings.pingsModelsForLatency` | zh-CN 多了 `{providerType}` 占位符，en 无此占位符 |

#### 硬编码 alert()（6 处）

| # | 文件 | 内容 |
|---|---|------|
| 1 | `BranchPanel.svelte:84` | `'Failed to delete branch'` |
| 2 | `BranchPanel.svelte:129` | `'Failed to create branch'` |
| 3 | `StoryEntry.svelte:220` | `'Cannot branch from this entry...'` |
| 4 | `StoryEntry.svelte:229` | `'Failed to create branch'` |
| 5 | `StoryEntry.svelte:271` | `'Failed to create checkpoint'` |
| 6 | `StoryEntry.svelte:974` | `'TTS is not enabled...'` |

### 🟡 P2 建议（110+ 个）

| 类别 | 数量 | 说明 |
|------|------|------|
| 硬编码 title 属性 | ~52 | `title="Cancel"` / `title="Delete"` 等 |
| 硬编码 placeholder | ~40+ | `placeholder="Select size"` / `placeholder="Enter API key"` 等 |
| 硬编码 aria-label | ~14 | `aria-label="Close notification"` 等 |
| 硬编码按钮/标签 | ~10+ | `Cancel` / `Confirm selection` 等 |
| ICU select 全角冒号 | 2 | `memory.deleteChapterConfirm` / `memory.chapterInfo` |
| 值与英文相同 | 3 | 品牌名/技术术语，可接受 |

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 为 `actionChoices.quickSelectHint` 添加 en + zh-CN 翻译 | i18n 开发 | P0 | 立即 |
| 2 | 为其余 13 个缺失键添加 en + zh-CN 翻译 | i18n 开发 | P1 | 本迭代 |
| 3 | 修复 `settings.pingsModelsForLatency` 多余占位符 | i18n 开发 | P1 | 本迭代 |
| 4 | 将 6 处 alert() 替换为 `$_()` 调用并添加翻译 | i18n 开发 | P1 | 本迭代 |
| 5 | 检查 ICU select 全角冒号是否影响渲染 | i18n 开发 | P2 | 下迭代 |
| 6 | 逐步将 title 属性硬编码替换为 i18n | i18n 开发 | P2 | 持续 |
| 7 | 逐步将 placeholder 硬编码替换为 i18n | i18n 开发 | P2 | 持续 |
| 8 | 逐步将 aria-label 硬编码替换为 i18n | i18n 开发 | P2 | 持续 |
| 9 | 清理 447 个 en.json 存在但源码未直接引用的键（确认是否动态引用后处理） | i18n 开发 | P2 | 版本稳定后 |

---

## ⚠️ 待完善 / 已知局限

- 447 个 en.json 存在但源码未直接引用的键，需确认哪些是动态引用、哪些是已废弃
- 硬编码扫描基于模式匹配，可能存在少量误报或遗漏
- 未进行运行时 UI 截图验证（无法在本地启动 Tauri 应用）

---

## 📊 测试数据摘要

| 指标 | 值 |
|------|-----|
| 语言文件 | en.json、zh-CN.json |
| en.json 总键数 | 1334 |
| zh-CN.json 总键数 | 1334 |
| 键覆盖率 | 100%（无缺失） |
| 源码 i18n 调用数 | ~680+ |
| 整体健康度 | 82/100 |

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
